"""Compy daemon — voice/text code-search pipeline (parse -> grep -> reason).

Per `.agent/code-search-jarvis-spec.md`. This package ships the Python-side orchestrator
the Swift overlay and the VS Code companion extension talk to.
"""

__version__ = "0.1.0"
