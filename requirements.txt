# Compy daemon — Python dependencies
# Install with: pip install -r requirements.txt
tree-sitter>=0.21.0
tree-sitter-python>=0.21.0
networkx>=3.0
pytest>=8.0      # dev only — for running the test suite
